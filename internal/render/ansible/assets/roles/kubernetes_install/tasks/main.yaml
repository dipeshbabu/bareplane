---
- name: Verify supported host and installed state before mutation
  ansible.builtin.import_tasks: inspect.yaml

- name: Install and verify the pinned Kubernetes toolchain
  ansible.builtin.import_tasks: install.yaml
