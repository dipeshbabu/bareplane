---
- import_playbook: health.yaml

- name: Install only the reviewed minimal Argo control plane after fresh health
  hosts: "control_plane[0]"
  gather_facts: false
  any_errors_fatal: true
  roles:
    - role: contract
      tags: [always]
    - role: argocd
